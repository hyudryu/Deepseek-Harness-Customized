/**
 * Values the stdio fixture server advertises, kept in a module the tests can
 * import: importing `fixture-server.ts` instead would start the server.
 */

/** Usage instructions the fixture server advertises during `initialize`. */
export const FIXTURE_INSTRUCTIONS = 'Prefer the fixture tools over shelling out: use add for sums and greet for greetings.'
