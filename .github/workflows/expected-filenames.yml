name: Expected filenames

# CI DISABLED — this workflow no longer triggers.
#
# The original trigger block is preserved below as comments so it can be
# restored verbatim: uncomment it and delete the `on: {}` line.

# on:
#   pull_request:
#     paths:
#       - '*[gG][oO][lL][dD][eE][nN]*'
#       - '**/*[gG][oO][lL][dD][eE][nN]*'
#       - '!vendor/**'
on: {}


permissions:
  contents: read

env:
  # CI runs must never report to the production telemetry endpoint baked
  # into apps/cli/cordis.yml (AppCLIEntry disables the row when set).
  DSH_TELEMETRY_DISABLED: '1'

jobs:
  expected-filenames:
    name: no golden filenames
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Check tracked filenames
        run: scripts/check-expected-filenames.sh
